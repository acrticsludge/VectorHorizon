# CLAUDE.local.md — VectorHorizon Project Reference

## Project: Semantic World-Builder

Turn static 2D images into physically consistent 3D video environments via NVIDIA Cosmos 3. Users upload an image, use a virtual joystick to "drive" through it. Cosmos generates 3-second MP4s per direction. Persisted to Supabase, stored in Cloudflare R2.

## Structure

```
frontend/     ← Next.js 16 App Router (Vercel Hobby, static-optimized)
worker/       ← Cloudflare Worker (Hono router, all API logic)
```

**Critical:** Zero Vercel API routes. ALL backend calls route to the Cloudflare Worker. This eliminates Vercel serverless compute costs.

## Tech Stack

| Layer | Tech | Why |
|-------|------|-----|
| Auth | Clerk | JWT templates for Supabase RLS |
| DB | Supabase Postgres | RLS enforced, user_id from `auth.jwt()->>'sub'` |
| Storage | Cloudflare R2 | Zero egress, presigned uploads |
| AI | NVIDIA Cosmos 3 NIM | Physical AI video generation |
| Worker | Cloudflare Workers + Hono | JWT verify (jose), API orchestration |
| Frontend | Next.js App Router | Server Components by default |

## DB Schema

- `worlds` — user_id, name, initial_image_url, created_at
- `world_transitions` — world_id, user_id, direction, video_url, created_at

RLS: `auth.jwt()->>'sub' = user_id` on both tables. Worker uses service role key (bypasses RLS) but gates via Clerk JWT verification.

## Key Architecture Decisions

1. **No Infinite Horizon chaining in MVP** — single generation per direction, static baseline image
2. **4-direction joystick** — forward/backward/left/right (no orbit, no analog delta)
3. **Video crossfade** — double-buffered `<video>` elements with 300ms CSS opacity transition
4. **Input queue** — 200ms debounce, max 1 pending direction, AbortController cancels in-flight
5. **Middleware matcher** — `/dashboard/:path*`, `/world/:path*` ONLY (no explosion)
6. **Server Components by default** — only CanvasViewport + JoystickOverlay + ImageUploader are `"use client"`

## Env Vars Required

```
# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY

# Supabase
NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY

# Worker
SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
CLERK_JWKS_URL (e.g., https://<domain>.clerk.accounts.dev/.well-known/jwks.json)
NVAPI_KEY, COSMOS_API_URL

# R2
WORLD_ASSETS (binding name in wrangler.toml)

# Frontend
NEXT_PUBLIC_WORKER_URL
```

## Checklists to Run Before Ship

Performance, Mobile, Error Handling, API Security (all in `.opencode/skills/`).

## Known Gotchas

- Vercel middleware MUST have explicit matcher or Fluid CPU bills explode
- Supabase anon key on frontend (RLS-gated), service role key in worker only
- jose package for JWT verification (not jsonwebtoken — jose is Edge-compatible)
- Cosmos returns full MP4, not streamed — budget 3-5s latency per generation
- R2 video URLs are returned as keys (need public bucket or proxy route)

## Design System (Google Stitch — Technical Minimalism)

Generated by Stitch. Any UI modification MUST use these exact tokens.

### Colors

| Token | Hex | Usage |
|-------|-----|-------|
| `--surface` | `#131315` | Card/header backgrounds, navbars |
| `--surface-dim` | `#131315` | Viewport base |
| `--surface-container-lowest` | `#0e0e10` | Page background, root |
| `--surface-container-low` | `#1c1b1d` | Card fill, skeleton base |
| `--surface-container` | `#201f22` | Elevated surfaces |
| `--surface-container-high` | `#2a2a2c` | Active nav state, hover overlay |
| `--surface-container-highest` | `#353437` | Skeleton shimmer highlight, highest elevation |
| `--primary` | `#ffffff` | All text headings, CTAs, active states |
| `--on-surface` | `#e5e1e4` | Body text |
| `--on-surface-variant` | `#a1a1aa` (Tailwind `text-[#c4c7c8]`) | Secondary text, muted labels |
| `--secondary` | `#71717a` | Footer text, tertiary labels |
| `--outline` | `#8e9192` | Strong borders (hover states) |
| `--outline-variant` | `#27272a` (Tailwind `#444748`) | Standard hairline borders (`border border-[#27272a]`) |
| `--error` | `#ef4444` | Error states |
| `--error-container` | `#93000a` | Error background |

**Banned:** No gradients, no soft shadows, no purple/blue neons. No pure black (`#000000`).

### Typography

- **Font:** Geist (variable), loaded via next/font/google in layout.tsx
- **Scales (inline, no Tailwind utility classes):**
  - `display-lg`: `text-[48px] leading-[1.1] tracking-[-0.02em] font-semibold` — hero headline only
  - `headline-lg`: `text-[32px] leading-[1.2] tracking-[-0.01em] font-medium` — section headings
  - `title-md`: `text-[18px] leading-[1.4] font-medium` — card titles, nav links
  - `body-md`: `text-[14px] leading-[1.6] font-normal` — body text
  - `label-sm`: `text-[12px] leading-[1.0] tracking-[0.05em] font-medium uppercase` — chips, status badges, footers
  - `code-sm`: `text-[13px] leading-[1.5] font-normal` — technical labels, depth params

### Spacing & Border Radius

- `px-gutter`: `16px` (use `px-[16px]`)
- `py-margin`: `24px` (use `py-6`)
- Section gaps: `py-8`, `py-10`, `py-32`
- Cards: `p-2` to `p-4`, `gap-3`
- Feature cards: `p-10`, `space-y-4`
- Radius: `rounded` = `0.25rem`, `rounded-lg` = `0.5rem`, `rounded-xl` = `0.75rem`, `rounded-full` = circular
- **Never use Tailwind `shadow` classes** — no shadows permitted by design system

### Interactive States

- Hover border: `hover:border-[#52525b]`
- Active press: `active:scale-95` or `active:scale-[0.98]`
- Transitions: `transition-all duration-200` or `duration-300` for cards
- Focus: implicit (no custom ring needed)

### Animations (CSS-only, minimal JS)

- **Shimmer skeleton:** `@keyframes shimmer` — `background: linear-gradient(90deg, #2a2a2c 25%, #353537 50%, #2a2a2c 75%)` at `background-size: 400% 100%`, `animation: shimmer 2s infinite linear`
- **Generation pulse:** `@keyframes pulse-subtle` — opacity 1↔0.7, `2s cubic-bezier(0.4,0,0.6,1) infinite`
- **Directional blur:** `filter: blur(8px)` with `transition: filter 0.5s ease`
- **Toast slide-up:** `transform translate-y-4 → translate-y-0` with `opacity 0 → 1`, `transition-all duration-300`

### Material Symbols Icons

CDN loaded. Usage: `<span class="material-symbols-outlined">icon_name</span>` with `font-variation-settings: "FILL" 0, "wght" 400, "GRAD" 0, "opsz" 24`. Size via `text-sm`, `text-2xl`, `text-3xl`. Active fill: `"FILL" 1`.

### Component Patterns

- **Navbar:** Fixed top, `h-16`, `bg-[#131315] border-b border-[#27272a]`, links: `text-[#a1a1aa] hover:text-white`
- **Sidebar:** Fixed left, `w-64`, `top-16`, `bg-[#131315] border-r border-[#27272a]`, active item: `bg-[#2a2a2c] text-white`
- **Cards:** `bg-[#1c1b1d] border border-[#27272a] rounded-lg hover:border-[#52525b]`, thumb: `aspect-video rounded overflow-hidden`
- **Buttons primary:** `bg-white text-black` (inverted in dark), `px-4 py-2 rounded`
- **Buttons CTA:** `bg-white text-black px-8 py-3 rounded` or full-width `max-w-xs`
- **Empty states:** `border-dashed border-[#27272a] rounded-xl bg-[#0e0e10]` with icon + title + description + CTA button

### Stitch-to-Code Conversion Rules

1. All `href="#"` → proper Next.js `<Link href="/real-path">`
2. All `<script>` tags → `"use client"` React components or removed
3. All CDN Tailwind `<script>` → already handled by project's Tailwind v4 + `postcss.config.mjs`
4. All `onclick="fn()"` → React `onClick={fn}` in client components
5. Hardcoded images → props (`src={initialImageUrl}`)
6. Hardcoded text labels → dynamic content or props
7. Duplicated layout (navbar, sidebar, footer) → extracted to shared components
8. Dead links (Generator, Assets, Settings, Help, Status) → REMOVED per product spec
